# J1 模拟器与 lint 资格记录（2026-09-02）

## 范围与代码基线

- Starting HEAD：`7243e211f023440d365ee1a28c1a432101e7445e`
- 先前 Settings harness remediation：`c56d0ffd6dc30ba485a91b3b57e2f1ac9b02c2eb`
- 先前 Manual harness remediation：`9c49e4bed2b59c91db4f9dff364136ed6c99dae5`
- Lifecycle-R1：`a15fba3bf8438275415c736d5f1f551b78978907`
- UI-R2：`dd5b4b4f03ce72451b0b0f2a9f7ca3de5890a1b1`
- WeekOverview-R1：`678af9884675cbc3f566b2963f14dfcd144c400d`
- J1 androidTest：`de600dde769c4482c002abb07f6d7fe4f1d180ff`
- J1 WIP 审计提交：`a757fc9d9770ba57ca9359f250b44e057b24c075`（临时分支内容已由正式 J1 commit 吸收，临时分支已删除）

J1 测试提交只增加 `app/src/androidTest` 与 test-only Gradle runner 配置；从 WeekOverview-R1 到 J1 test commit，`app/src/main` 与 `app/src/debug` 均为零变化。Lifecycle-R1、UI-R2、WeekOverview-R1 是 J1 继续前分别获批并完成 TDD/回归的产品修正，不属于 androidTest harness 内联修复。

## 工具链

- OS：Windows 11 10.0 amd64
- JDK：Eclipse Adoptium 21.0.12.1
- Gradle：9.5.0
- Android Gradle Plugin：9.3.0
- Project Kotlin catalog：2.4.10（Gradle embedded Kotlin 2.3.20）
- Compose BOM：2026.06.00
- compileSdk / targetSdk / minSdk：37 / 36 / 26
- AndroidX Test ext JUnit / runner：1.3.0 / 1.7.0

## 模拟器

- AVD：`dsh_android16`
- Serial：`emulator-5554`
- Model：`sdk_gphone64_x86_64`
- API：36
- ABI：x86_64
- 本轮启动方式：cold boot，禁用 snapshot/boot animation/audio；测试结束恢复动画倍率并关闭本轮启动的实例。

## JVM 单元测试与 lint

- Fresh serial full suite：887/887，failures=0，errors=0，skipped=0。
- Fresh default-worker full suite：887/887，failures=0，errors=0，skipped=0。
- 先前稳定性门：Settings class 与 Manual class focused 组合已通过；本轮未改这两个测试文件。
- `lintDebug`：errors=0，warnings=12。
- Warning IDs：`AndroidGradlePluginVersion` 2、`ComposableNaming` 2、`DataExtractionRules` 1、`GradleDependency` 1、`InlinedApi` 1、`MissingApplicationIcon` 1、`OldTargetApi` 1、`SetJavaScriptEnabled` 1、`UseKtx` 2。
- Reports：`app/build/reports/lint-results-debug.xml`、`app/build/reports/lint-results-debug.html`。

## Connected qualification

Test-owned `J1AndroidJUnitRunner` 强制 target application 为基础 `TimetableApp`，不自动运行 `DebugTimetableApp` / `DebugSeed`。完整 Activity 测试使用 `createEmptyComposeRule`，先 reset/seed，再启动 `ActivityScenario<MainActivity>`；每个测试在下次 Room reset 前关闭 scenario。DebugSeed 仅由测试显式调用。Reauth 使用 androidTest-only sequenced runner fake，不触达真实门户或 WebView。

Targeted classes：

- `J1RuntimeIsolationTest`：1/1。
- `TimetableSmokeTest`：4/4。
- `ManualItemUiTest`：5/5。
- `ReauthUiTest`：3/3。

Fresh full connected suite：13/13，failures=0，errors=0，skipped=0，XML 记录耗时 66.486 秒。

- XML：`app/build/outputs/androidTest-results/connected/debug/TEST-dsh_android16(AVD) - 16-_app-.xml`
- HTML：`app/build/reports/androidTests/connected/debug/index.html`

覆盖：基础 Application 隔离、空库手动 fallback、七日课表、显式 DebugSeed、切周/滑动、学期 viewed-only 切换、Settings/Profile 导航、手动项目创建/编辑/确认删除且 SCHOOL 行不变、认证失效保留 grid、重登成功精确重试一次、取消不重试，以及 WeekOverview 的 active-only 周内容与展开状态行为。

## 构建与 release 隔离

- `assembleDebug`：GREEN；`app/build/outputs/apk/debug/app-debug.apk`，14,682,501 bytes。
- `assembleRelease`：GREEN；`app/build/outputs/apk/release/app-release.apk`，10,583,439 bytes。
- release intermediates 中 J1 runner/test class 命中：0。
- `apkanalyzer dex packages --defined-only` 对 release APK 的 J1 runner/test class 命中：0。

## 边界与未覆盖项

- 本轮 production 变化只位于 MainActivity ownership、课表/学期/设置视觉层级与 WeekOverview；无 Room schema、repository、sync、portal 或 Settings authority 变化。
- 新增真实 portal URL / selector / WebView runtime：0。
- Real portal evidence：9/9 PENDING；真实 USTC 登录、parser 与 periodic sync runtime 均未宣称验证。
- 模拟器已覆盖 safe-area 与 compact geometry 的自动化/单元行为；真实设备全屏与小窗仍未执行，留给 J2。
- J2 NOT STARTED；J3 NOT STARTED。

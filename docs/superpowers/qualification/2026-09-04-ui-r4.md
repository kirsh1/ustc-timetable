# UI-R4 viewport and appearance qualification — 2026-09-04

The original qualification below is retained as historical evidence. The **UI-R4 addendum qualification — 2026-09-05** section records the later approved 18-task wave; its results must not be confused with the original 1042-test run.

## Scope and baseline

- Starting HEAD: `dadf17b1a89796d01fefb26c0c4310266c409cb3`
- Implementation commit: `c3d63da`
- Connected-test commit: `a329292`
- Scope: fixed time rail, unified segmented axis, complete period boundaries, course-card hierarchy, LIGHT/DARK/SYSTEM appearance, timetable-only wallpaper.
- Real portal, Room schema, sync target authority, WebView login, and WorkManager semantics were not changed.

## Root-cause record

- The time gutter lived inside each `HorizontalPager` page, so it participated in week swipes.
- teaching-group detection used a 20-minute boundary while compression used a greater-than-30-minute boundary.
- the gutter's grouped rendering omitted individual period boundaries.
- logical course bounds and painted card bounds were identical, obscuring adjacent grid lines.
- the top bar placed its weighted spacer before the next arrow and used different containers for week and date.
- the outer theme was fixed to light; a background `Box` did not provide `onSurface`, leaving inherited controls dark in dark mode.

## TDD evidence

Initial feature RED was caused by missing `AppearanceMode`, wallpaper state, `TimeBoundaryMark`, fixed-rail composition, and card visual bounds. Device-feedback RED then independently locked:

- symmetric previous/next spacing;
- a 4dp time-label inset;
- dark content color inheritance for timetable and non-timetable destinations;
- neutral theme-derived teaching-gap color.

All corresponding targeted tests passed after the minimal implementations.

## Behavioral evidence

- `FixedTimeRail` is a sibling before `HorizontalPager`; weekday/date headers and grid remain within each pager page.
- the official four teaching-group gaps resolve to the same requested visual height; inverse time mapping continues through the same reversible axis.
- all 13 period starts/ends are represented, including adjacent 5-minute boundaries and `21:55`; custom profile times remain the authority.
- course cards inset their painted rectangle by 1dp without changing their logical interval; text priority is name, location, teacher, time.
- top-bar semantic bounds on the API 36 emulator showed equal 54px visual gaps around the week/date group and matching y=202px text centers.
- LIGHT is the default. LIGHT, DARK, and SYSTEM are persisted through `appearance_mode`.
- wallpaper uses Photo Picker, persists only the returned URI in `timetable_wallpaper_uri`, requests a persistable read grant, applies a theme-specific scrim, and falls back to the solid theme surface if unreadable.
- an androidTest-owned provider verified wallpaper rendering on timetable, absence on Settings, and unreadable-URI fallback without reading the user's gallery.

## Qualification results

- JVM serial: 1042 tests, 0 failures, 0 errors, 0 skipped.
- JVM default workers: GREEN.
- lintDebug: 0 errors, 12 warnings (`AndroidGradlePluginVersion` 2, `ComposableNaming` 2, `DataExtractionRules` 1, `GradleDependency` 1, `InlinedApi` 1, `MissingApplicationIcon` 1, `OldTargetApi` 1, `SetJavaScriptEnabled` 1, `UseKtx` 2).
- connectedDebugAndroidTest: 17 tests, 0 failures, 0 errors, 0 skipped; 104.941 seconds in the final XML.
- assembleDebug: GREEN; `app-debug.apk` 21,695,371 bytes.
- assembleRelease: GREEN; `app-release.apk` 14,892,819 bytes.

## Environment

- Gradle 9.5.0; AGP 9.3.0; Kotlin 2.3.20; JDK 21.0.12.1.
- compileSdk 37; targetSdk 36; minSdk 26.
- AVD `dsh_android16`, serial `emulator-5554`, model `sdk_gphone64_x86_64`, API 36, ABI x86_64.
- Emulator screenshots confirmed corrected light and dark timetable rendering. A physical-device wallpaper and freeform-window screenshot remain for a later device-only acceptance pass.

## Evidence paths

- Unit XML: `app/build/test-results/testDebugUnitTest/`
- Lint XML/HTML: `app/build/reports/lint-results-debug.xml`, `app/build/reports/lint-results-debug.html`
- Connected XML/report: `app/build/outputs/androidTest-results/connected/debug/`, `app/build/reports/androidTests/connected/debug/`
- APKs: `app/build/outputs/apk/debug/app-debug.apk`, `app/build/outputs/apk/release/app-release.apk`

---

## UI-R4 addendum qualification — 2026-09-05

### Authorities and execution

- Approved application baseline: `75b15822890e379b3c3382bdf95a76b539065f4b` (`f7cb33b^`).
- Approved plan / original execution starting HEAD: `fa0937b32e50a1b41ad3badf4bf6e494a45d5d69`.
- Resume starting HEAD: `b547a4166913e17d24ef84a0001d0982b8dc070c` (Tasks 1–10 complete); clean tree and empty stash.
- Qualified application/test tree: `1b17340a3f30b78cf8a782253589f6965829ec3f`.
- Scope is the approved spec r1 and plan r1.1, with the user's subsequent explicit correction allowing location codes to wrap instead of enforcing a single line.
- No new intermediate approval gates were introduced. Qualification is not an additional implementation Task.

### Task commits and internal execution checklist

Each row records the six required internal steps: failing test written, RED observed, implementation, targeted GREEN, regression, commit. Historical Tasks 1–10 were completed before this resume; their individual run counts are not reconstructed from the current build directory. Final full qualification covers their resulting tree again.

| Task | Commit | Capability | Six-step execution record |
|---|---|---|---|
| 1 | `a35a81c` | Automatic end-date model | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 2 | `7c6c7b0` | AUTO/MANUAL end-date UI | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 3 | `baf169d` | Week/date optical centering | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 4 | `edd0c98` | Card text hierarchy | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 5 | `f73ab08` | Measured horizontal rail | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 6 | `5b1399b` | Gesture state machine | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 7 | `097e7cf` | Compose gesture arbitration | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 8 | `3ddbc4c` | SCHOOL ghost projection | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 9 | `fa26e91` | SCHOOL marker rendering | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 10 | `b547a41` | Stable detail-page model | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 11 | `7d89926` | Detail pager | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 12 | `c71f3b5` | Two preference keys | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 13 | `9f66eea` | Seeded palette and generator | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 14 | `5b0c23b` | Wallpaper editor model | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 15 | `4a7d768` | Palette candidate/apply UI | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 16 | `2c7b1e9` | Wallpaper strength UI | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 17 | `44dbf9b` | Cross-renderer appearance wiring | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |
| 18 | `c7d7c24` | Connected behavior and semantics | [x] Test [x] RED [x] Implement [x] GREEN [x] Regression [x] Commit |

Total: 18 Tasks, 108 internal completion marks. Additional harness commits: `01a29ae` (semester sheet isolation), `71816e0` (actual measured text priority regression), `90a2a39` (wait for both Activity font configuration and rendered rail growth before capture), and `1b17340` (window-event quiescence before device capture). They do not introduce feature Tasks or external gates.

### RED/GREEN evidence and deviations

- Detail-pager target: 41 passing; complete gesture/ghost/detail subplan regression: 225 passing across 13 classes.
- Preference target: 5 passing. Windows JVM DataStore rename behavior required a Robolectric application-backed harness; real multiple-write assertions were retained.
- Palette editor/theme target: 11 passing; wallpaper editor/state target: 7 passing. Palette settings target: 55 passing; wallpaper integration regression: 33 passing.
- Appearance wiring target: 53 passing. Its first combined regression had 148 tests with one `ManualItemFlowTest.ghost_manual_click_preserves_original_week_pattern` wait timeout. Diagnostic state/row reporting was added without changing the timeout or behavior; the diagnostic run passed 148/148. The intermittent timeout's original cause was not established and is not claimed fixed. Final full-suite evidence is separately reported below.
- Initial connected RED exposed absent marker semantics and a real slider cancellation bug. Material Slider invokes a shared finish callback after cancellation; interaction cancellation is now consumed synchronously without persisting the candidate. The device test sends an actual pointer cancel and checks stored state.
- Existing reauth instrumentation clicked an offscreen sync row after appearance controls increased the list height. The harness now scrolls to the row and verifies visibility before clicking. No sync production code changed.
- Review RED exposed title-budget priority and non-monotonic wallpaper visibility. Title lines are measured rather than always reserving three; remaining location lines precede teacher/time allocation. Wallpaper alpha uses an independent Float graphics layer and monotonic safety-scrim curve `s*v/(1-s+s*v)`, preserving zero and full-strength endpoints. Targeted correction: 53/53 passing. Review reported no remaining blockers after verification.
- User location correction: `TH-A301-ROOM` is verified on-device with more than one rendered line, no ellipsis when height permits, and its text bottom above the marker footer. Location remains monospace/SemiBold; card time and teacher content yield space first. The 9dp marker footer is reserved in both measurement and rendering.
- Final Task 18 combined device run before qualification: 23/23, zero failures/errors/skips (8 new UI-R4, 5 smoke, 5 MANUAL, 2 wallpaper, 3 reauth).
- First fresh full unit qualification: 1144 tests, one obsolete `course_name_and_location_have_priority_over_teacher` fixture failed. Native glyph measurement alone still reproduced it: a 90-minute card legitimately has teacher space when its title needs one line. The scarcity fixture now uses 60 minutes and native rendering; assertions still require title and location present and teacher absent. Its grid/text/integration regression passed 53/53. No production workaround or timeout increase was used.
- Fresh approved targeted gate: 89/89. Fresh frozen-axis gate: 57/57. All had zero failures/errors/skips.
- Screenshot review rejected the initial font-scale image because it matched the normal-size layout. Waiting for system configuration alone was insufficient: a rendered-layout condition is also required before capture. The strengthened instrumentation case passed and produced visibly enlarged labels with the full rail and seven days present. No production font behavior was changed.

### Compatibility and boundary audit

- `SettingsStore` adds exactly `course_palette_seed: Long` (missing = `0L`) and `wallpaper_visibility_percent: Int` (missing = 65; reads and writes clamp to 0–100). Existing appearance-mode and wallpaper-URI keys are unchanged. No Room migration.
- Seed 0 keeps the existing palette mapping. Explicit new-candidate actions alone call the injected seed source; a collision advances deterministically to a different palette variant. Apply alone persists palette state; restore-default remains a candidate action.
- Wallpaper drag previews locally, a successful finish persists, and cancellation restores persisted state. It changes only the wallpaper/scrim layers, never course-card or content alpha.
- SCHOOL attachment and carousel use stable course identity and canonical presentation keys, never local meeting ID for ordering. Each stable course occupies one page, with the current representative first. WeekPattern-only differences do not create a variant marker. MANUAL persistence/editor/ghost behavior is isolated.
- Overview expansion remains non-persistent. Gesture state arbitration preserves horizontal paging, consumes card long presses, suppresses clicks/long presses after over-slop diagonal motion, and reserves empty-area long press for manual creation.
- Approved-baseline diff exits 0 for Room entities/DAOs/schema, domain, school/portal, sync, manual persistence, schedule profile, main manifest, debug and release source sets.
- Approved-baseline diff exits 0 for `TimelineAxis.kt`, `SegmentedTimelineAxis.kt`, `LongPressResolver.kt`, and `TimeBoundaryMark.kt`. Only horizontal rail measurement changed; no Y-axis redesign.
- This wave used test-owned emulator data. It did not collect credentials or raw portal evidence, exercise real portal login/import, or claim real portal/Worker qualification.

### Current environment

JDK 21.0.12.1; Gradle 9.5.0; AGP 9.3.0; Kotlin 2.4.10; Compose BOM 2026.06.00; compileSdk 37 / targetSdk 36 / minSdk 26. AVD `dsh_android16`, serial `emulator-5554`, model `sdk_gphone64_x86_64`, API 36, ABI x86_64. TEMP/TMP `C:\jtmp`; `GRADLE_OPTS=-Djava.net.preferIPv4Stack=true`; one worker. Original emulator font scale 1.0 and all three animation scales 0.

### Qualification commands and immutable-tree applicability

The full fresh gate command at `71816e0` completed successfully (8m 57s):

```powershell
$env:TEMP = 'C:\jtmp'
$env:TMP = 'C:\jtmp'
$env:GRADLE_OPTS = '-Djava.net.preferIPv4Stack=true'
./gradlew :app:testDebugUnitTest :app:lintDebug :app:connectedDebugAndroidTest :app:assembleDebug :app:assembleRelease "-Pandroid.testInstrumentationRunnerArguments.uiR4Screenshots=true" --rerun-tasks --max-workers=1
```

- Full serial unit XML: **1144 tests, 0 failures, 0 errors, 0 skipped**. This includes `ManualItemFlowTest` 26/26.
- Initial full connected gate: **25 tests, 0 failures, 0 errors, 0 skipped** (140.401 seconds). This was repeated after strengthening capture synchronization; final counts are recorded below.
- Debug APK: `X:\schedule\app\build\outputs\apk\debug\app-debug.apk`, **21,793,675 bytes**.
- Release APK: `X:\schedule\app\build\outputs\apk\release\app-release.apk`, **14,958,355 bytes**. Signing configuration remains the pre-existing debug signing configuration; this is not a distribution-signing qualification.
- Actual release DEX scanned with `apkanalyzer.bat dex packages --defined-only app/build/outputs/apk/release/app-release.apk`: command exit 0; zero matches for `UiR4ConnectedTest`, `J1MainActivityHarness`, `TimetableSmokeTest`, `ManualItemUiTest`, `ReauthUiTest`, and `J1AndroidJUnitRunner`. Release-intermediate test-class matches: 0.
- Subsequent changes are androidTest screenshot synchronization only. `git diff --exit-code 71816e0..HEAD -- app/src/main app/src/test app/src/debug app/src/release app/build.gradle.kts gradle` exits 0, so full unit/build/DEX results apply to the identical final production and JVM-test tree. Lint and the complete connected suite were rerun with the changed instrumentation.

Final instrumentation/lint command, followed by exact-build installation:

```powershell
./gradlew :app:lintDebug :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.uiR4Screenshots=true" --max-workers=1
./gradlew :app:installDebug
```

Gradle reevaluates the changed androidTest inputs and the connected task. The screenshot argument only enables a helper under `androidTest`; production contains no screenshot switch. Capture waits for Compose idle, settled window events, and (for enlarged-font evidence) both the Activity font configuration and an actually wider rendered rail. Each PNG is written with device-side `screencap -p /sdcard/Pictures/ui-r4-<name>.png` and retrieved with `adb -s emulator-5554 pull`; no PowerShell binary redirection is used. Device screenshot copies are disposable and removed after successful pulls; repository copies are retained.

AGP emits a `testsuites` XML root for connected results, unlike the unit `testsuite` root. Final connected totals are read from that root, not interpreted as zero when the singular property is absent.

### Final results and visual review status

- Implementation: **18/18 complete**; internal execution marks **108/108**.
- Automated qualification: **PASS**. Final connected XML: **25 tests, 0 failures, 0 errors, 0 skipped**, 176.708 seconds; final command exit 0. Full serial unit: **1144/1144** as above.
- Final lint: **0 errors, 12 warnings**: `AndroidGradlePluginVersion` 2, `ComposableNaming` 2, `DataExtractionRules` 1, `GradleDependency` 1, `InlinedApi` 1, `MissingApplicationIcon` 1, `OldTargetApi` 1, `SetJavaScriptEnabled` 1, `UseKtx` 2.
- Both builds and exact debug installation: successful. Release DEX and frozen production-boundary audits: passing.
- Visual qualification: **REVIEW REQUIRED**, not an unconditional visual PASS. All 12 device screenshots were pulled and inspected. Application content demonstrates room wrapping, separate marker space, current-course-first pages, single-page variants, palette controls and distinct wallpaper strengths. Enlarged font visibly increases rail width while retaining seven days.
- Two screenshot observations remain: some dark/theme/font-transition captures have low-contrast system status/navigation glyphs; the expanded-overview captures do not visibly show the terminal `21:55` rail label, although collapsed captures do. The automated geometry tests do not establish that every final compositor frame shows that label. These are recorded without claiming an emulator-only cause or changing frozen vertical semantics. The existing system-bar code is in `MainActivity.kt`; the complete-boundary rendering belongs to the earlier fixed-axis UI-R4 baseline. A focused visual diagnosis is needed before declaring unconditional visual acceptance.
- At font scale 1.3, long titles can reach the approved three-line cap and ellipsize; room codes wrap and remain visible. This is not a claim that arbitrary text can always fit every card.
- Test fixture wallpaper is a uniform test-owned image, not a user's gallery image. No physical-device or real-portal acceptance is claimed.

Screenshot directory: `X:\schedule\docs\superpowers\qualification\2026-09-04-ui-r4-screens\`.

| Evidence | PNG |
|---|---|
| Light timetable | `light-collapsed.png` |
| Light overview | `light-overview-expanded.png` |
| Dark timetable | `dark-collapsed.png` |
| Dark overview | `dark-overview-expanded.png` |
| Current course, page 1 | `different-course-page-1.png` |
| Other course, page 2 | `different-course-page-2.png` |
| Same course / full arrangements | `same-course-single-page.png` |
| Palette candidate | `palette-candidate-sheet.png` |
| Wallpaper hidden | `wallpaper-0-percent.png` |
| Wallpaper 65% | `wallpaper-65-percent.png` |
| Wallpaper 100% | `wallpaper-100-percent.png` |
| Font scale 1.3 | `font-scale-1-3-measured-rail.png` |
